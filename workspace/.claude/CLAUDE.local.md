# CLAUDE.local.md — personal, uploaded on push

Your personal notes for this Agnes workspace. Loaded automatically alongside `CLAUDE.md`. Uploaded to the server on `agnes push` (and on SessionEnd).

This file is preserved across `agnes init --force`.

Add anything that helps your future self or the corporate-memory pipeline: working hypotheses, shortcut commands, snapshot names you keep reusing, gotchas you ran into.
